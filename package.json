{
  "devDependencies": {
    "globals": "^15.8.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.3"
    ,"eslint": "^9.6.0"
  },
  "scripts": {
    "c": "ts-node --transpile-only --project ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    
  },
  "main": "index.hrml",
  "workspaces": [ ".vscode/tsconfig.json" ],
  "config": {
    "watch": true,
   }
  }

